{
	"name": "@bt/lib",
	"version": "1.7.0",
	"scripts": {
		"rm-build": "rimraf js",
		"wait-on-build": "wait-on js",
		"build": "npm run rm-build && tsc && npm run wait-on-build"
	},
	"devDependencies": {
		"@types/node": "^22.15.14"
	}
}
