{
	"name": "@bt/lib",
	"version": "1.7.0",
	"scripts": {
		"rm-build": "tsc --build --clean",
		"wait-on-build": "wait-on js/index.js",
		"build": "npm run rm-build && tsc && npm run wait-on-build"
	}
}